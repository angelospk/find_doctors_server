# Optional: only needed for fully-headless TaxisNet auto-login when the gsis.gr SSO
# has fully expired. Leave unset to log in manually once in the bridge window.
# Stored locally only — .env is gitignored, never commit real values.
TAXISNET_USERNAME=
TAXISNET_PASSWORD=
